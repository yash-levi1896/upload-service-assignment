{
  "name": "problem-4-csv-import-processing",
  "version": "1.0.0",
  "description": "Large CSV upload with async background processing (Express + PostgreSQL + Redis/BullMQ). API server only uploads + enqueues; a separate worker process streams, validates, batches, and inserts.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "worker": "node worker.js",
    "worker:dev": "nodemon worker.js",
    "migrate": "node db/migrate.js"
  },
  "dependencies": {
    "bullmq": "^5.12.0",
    "busboy": "^1.6.0",
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "ioredis": "^5.4.1",
    "pg": "^8.12.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
